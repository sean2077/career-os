# v0.7.1 Release Evidence

Date: 2026-08-23

Release boundary: the reviewed public change
`e806707cfc06843e8c3c4f8ed72dad51f20f5395` applies the outcome-first
context optimization on top of public `v0.7.0` commit
`2f3063e7bdd3583cc17bc12aeef57d5688c90a26`.

The public snapshot retains `standalone-framework`, the light Agent harness,
synthetic-only fixtures, and the existing tag-triggered release workflow. The
same behavior was subsequently adapted into the private integrated workbench;
no private record, identity, attachment, font, Obsidian state, runtime state,
or generated output enters this release.

## Scope

- Keep high-frequency authority, ownership, authorization, schema, privacy,
  and validation rules in the root contract while routing lower-frequency
  procedures to focused documentation.
- Add a minimum-context ladder, answer-only versus durable persistence rules,
  one-time post-write validation, reviewer and research stopping rules, and
  stable-ID cross-authority handoffs.
- Add nine on-demand outcome-first workflows without introducing an eighth
  Skill, another authority, new record kinds, a workflow DSL, or Agent-side
  context cache.
- Rewrite all seven Career Skills around bounded fast paths and expand Career
  Communication to cover application answers, networking and recruiter
  messages, unsent negotiation wording, and evidence audits.
- Align `role-market.channel` and `career-communication.audit` across the Skill
  catalog, executable mode matrix, and hidden blind-selection oracle.

## Candidate verification

| Gate | Result |
| --- | --- |
| Offline locked dependency resolution | PASS |
| `career-os check --fast` | PASS with the documented blind-selection ATTENTION only |
| `career-os check` | PASS with the documented blind-selection ATTENTION only |
| Full Python suite | PASS |
| `ruff check .` | PASS |
| `mypy system/tools/career_os` | PASS |
| `career-os skills verify` | PASS with the documented blind-selection ATTENTION only |
| Exact `v0.7.1` changelog-note extraction | PASS |
| Complete extraction-supplement verification | PASS |
| Clean isolated package build and reviewed light Harness | CI-GATED because the local PyPI tunnel was unavailable; required on `main` before tagging |

The complete public-history privacy audit with the private Career Home
comparison runs against the clean release commit before any public push. The
tag workflow repeats the public-only privacy, repository, resume, and release
gates before creating the GitHub Release.

The immutable
[v0.7.1 extraction supplement](v0.7.1-extraction.json) records the direct
public-native delta over v0.7.0 and hash-binds the complete release snapshot.

Automation establishes deterministic framework, Skill routing, privacy, and
release behavior. It does not establish career evidence maturity, readiness,
application success, or any external career outcome.
