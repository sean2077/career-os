# v0.3.0 Release Evidence

Date: 2026-07-24

Release boundary: public framework changes were extracted from clean Career
Home commit `15628ed54661b26d16fb083f5f8bc74ff5671017`, adapted on top of
public `v0.2.0` commit `d0bfb1fa5bf8e139b2ec5f9d2417a3344151da92`,
and reviewed as five public-only commits ending at
`e87f0afb51d9171c1a1d98f6c595d3a3ede04c7f`. Clean-clone stabilization then
repaired schema-2 relationship migration at
`279000aeac6666faef921db26e18e72856d89e80`. Publication pushes the final release
commit to `main`, waits for that exact SHA's CI, and only then pushes the
annotated `v0.3.0` tag.

Compatibility boundary: this is a breaking pre-1.0 release. Defuddle and
OpenCLI are no longer shipped. Existing `[research.opencli]` configuration
fails closed and must be removed. Project schema 2, record schema 3, fixed
`career/`, and fixed `.career-os/runtime/` remain unchanged.

## Public extraction

The contribution was produced from reviewed framework-only patches; no Home
commit or parent was merged, rebased, cherry-picked, or made reachable from the
public repository. Public adaptation retains
`development_topology = "standalone-framework"`, public homepage names, system
font defaults, public CI/release guards, and synthetic fixtures.

Personal career records, identity, attachments, local fonts, `.obsidian/`,
runtime, build, and `.career-os/` state are excluded. The immutable
[v0.3.0 extraction supplement](v0.3.0-extraction.json) records the exact
changed and deleted public paths on top of the retained v0.1.0 extraction
evidence.

The changed public CI workflow was deliberately reviewed as synthetic-only
before its new hash was appended to
`system/privacy/public-fixture-policy.json`.

## Release surface

- Removed Defuddle/OpenCLI Skills, configuration, runtime, tests, attribution,
  and supply-chain entries.
- Added paired Recent Changes Bases and six localized Base embeds per homepage.
- Composed identified-employer JD screening with canonical Company research.
- Preserved Base localization parity while normalizing YAML.
- Consolidated redundant tests and documented the test-cost budget.
- Restored schema-2 relationship vocabulary migration and valid optional
  Market Channel relations discovered by the clean-clone journey.

## Candidate verification

| Gate | Result |
| --- | --- |
| `uv sync --locked --all-groups` | PASS |
| `uv run career-os check --fast` | PASS with the documented blind-selection ATTENTION only |
| `uv run career-os check` | PASS with the documented blind-selection ATTENTION only |
| `uv run pytest` | PASS |
| `uv run ruff check .` | PASS |
| `uv run mypy system/tools/career_os` | PASS |
| `uv run career-os skills verify` | PASS with the documented blind-selection ATTENTION only |
| `uv run career-os views build` | PASS, two homepages and eighteen tracked assets |
| Clean-clone initialization, schema-2 fictional graph migration, apply, verify, and full check | PASS |
| Complete public-history privacy audit | PASS |
| Redacted private Career Home comparison | PASS |
| JSON Schemas, NOTICE, Skill locks, and deterministic SBOM | PASS |
| Light dual-host Agent harness | PASS |
| `career-os release notes --tag v0.3.0` | PASS |
| Git whitespace and prohibited-path checks | PASS |

## Evidence boundary

Automation did not run a real Codex or Claude Code behavioral selection,
application workflow, external research action, or live Obsidian interaction.
These gates establish deterministic framework, installation, and privacy
behavior; they do not establish career evidence maturity, readiness,
application success, or any external career outcome.

The tag-triggered workflow exclusively owns GitHub Release creation and reruns
the repository and synthetic resume gates before publication.
