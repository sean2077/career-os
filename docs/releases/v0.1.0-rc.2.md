# v0.1.0-rc.2 Release Evidence

Date: 2026-07-21

Platform: Windows 11, Python 3.12.13, uv 0.11.29, Git 2.54.0, TeX Live 2025

Release boundary: public GitHub prerelease created exclusively by the
tag-triggered Release workflow

## Delivered commits

| Area | Commit | Subject |
| --- | --- | --- |
| Career semantics | `cbb3fac` | `feat(core)!: enforce schema 2 career semantics` |
| Obsidian guides | `0468a06` | `feat(obsidian): publish native career Canvas guides` |
| Release automation | `c10a45d` | `ci(release): add tag-triggered publication` |

This evidence file is part of the release commit. The annotated
`v0.1.0-rc.2` tag points to that complete snapshot.

## Migration impact

Schema 2 adds kind-specific lifecycle fields and deterministic cross-authority
gates. Existing schema-1 user records are never changed by a system update.
Owners must review `career-os migrate plan --to 2`, then explicitly apply the
hash-bound plan or leave their data unchanged. Rollback remains available from
the operation receipt and backup.

## Local verification

| Gate | Result |
| --- | --- |
| `uv sync --locked --all-groups` | PASS, 28 packages checked |
| Ruff | PASS |
| mypy strict | PASS, 30 source files |
| pytest | PASS, 58 tests |
| `career-os skills verify` | PASS, exactly 7 Career + 2 Sean + 5 Obsidian Skills |
| `career-os check --fast` | PASS |
| `career-os check` | PASS, including native PNG and SBOM validation |
| `career-os release notes` | PASS, exact non-empty tag section extracted |
| light Agent harness verify | PASS, 16 checks including 14 real Skill symlinks |
| English and Simplified Chinese XeLaTeX build/export | PASS |

`skills.blind-selection` remains an explicit `attention` result until an
independent Agent receives only the prompt packet and returns a report. The
release does not mislabel fixture coverage as blind behavioral acceptance.

## Native Obsidian documentation images

Both README images are native Obsidian Full canvas PNG exports with logo and
privacy mode disabled because the framework Canvases contain no user data. The
Canvas sources remain canonical.

| Projection | Dimensions | SHA-256 |
| --- | ---: | --- |
| `career-map.png` | 5580 × 3030 | `c940a90560fa6a913c841256702f2b459f1ba82c0db74307854e3d6be38c2b51` |
| `career-guide.png` | 5970 × 3075 | `65a6a326a6b8a8bb73d9d1f60e93087d78a0bed695b4c7fea2125c6a61f19447` |

The PNGs passed full-size visual inspection and a binary scan for local paths,
usernames, file URIs, and Obsidian URIs.

## Publication contract

The public repository is
[`sean2077/career-os`](https://github.com/sean2077/career-os). Publication is
not complete when `main` or the tag is merely pushed. The Release workflow must
check out the tag commit, validate the exact changelog section, re-run the core,
Skill, harness, resume build, and privacy-export gates, and create a non-draft
GitHub prerelease that identifies the same tag. The hosted workflow run and
Release URL are verified after the immutable tag is pushed.
