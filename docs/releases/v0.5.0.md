# v0.5.0 Release Evidence

Date: 2026-07-25

Release boundary: the corrected public-bound homepage contract was reviewed
against clean Career Home commit
`b69928c3296c74c165709f4b796422ac765e8369`, adapted on top of public
`v0.4.0` commit `9d3c4c8e3a5b0a5838a2c348c53c8dedbc43a1c0`, and committed as the
public-only contribution
`413135af809498777007cb45d6bf2cb50ec5c335`.

The public adaptation retains `standalone-framework`, the light Agent harness,
the renamed root `Career Home.md` and `职业主页.md` assets, and synthetic-only
fixtures. It excludes Home worktree governance, Vault-local tasks, personal
resume sources, `career/`, identity, attachments, fonts, `.obsidian/`, runtime
state, and generated output.

## Scope

- Rename the English and Chinese root Obsidian entry points so they cannot be
  mistaken for the surrounding Vault's own homepage.
- Update current framework links, localized Recent Changes Bases, CLI output,
  deterministic validators, tests, and documentation together.
- Clarify that `general` is the default resume name while `preview` is the
  default export profile.
- Preserve historical release evidence and require external links or embeds to
  adopt the new root filenames explicitly.

## Candidate verification

| Gate | Result |
| --- | --- |
| `uv sync --locked --all-groups` | PASS |
| `uv run career-os check --fast` | PASS with the documented blind-selection ATTENTION only |
| `uv run career-os check` | PASS with the documented blind-selection ATTENTION only |
| `uv run pytest` | PASS, 224 tests |
| `uv run ruff check .` | PASS |
| `uv run mypy system/tools/career_os` | PASS |
| `uv run career-os skills verify` | PASS with the documented blind-selection ATTENTION only |
| Light dual-host Agent harness verification | PASS |
| Exact `v0.5.0` changelog-note extraction | PASS |
| Complete public-history privacy audit with redacted Career Home comparison | PASS |

The immutable
[v0.5.0 extraction supplement](v0.5.0-extraction.json) records the exact
Home-to-public result over v0.4.0.

Automation establishes deterministic framework, Obsidian, privacy, and release
behavior. It does not establish career evidence maturity, readiness,
application success, or any external career outcome.
