# v0.3.1 Release Evidence

Date: 2026-07-24

Release boundary: the immutable annotated `v0.3.0` tag points to
`bc635b6ee28986b80772e93abd1ead8e8a80bfc3`. Its tag workflow completed the
full Python suite and clean-checkout assertion, then failed because a separate
step still named the deliberately deleted redundant
`system/tests/test_golden_journey.py` wrapper. No GitHub Release was created.

v0.3.1 rolls forward without moving, deleting, or reusing that tag. The stale
workflow invocation is removed; the complete suite, clean-checkout assertion,
full repository checks, privacy history, Skill and harness validation, and
synthetic XeLaTeX build/export audits remain release requirements.

## Verification

The patch release repeats the complete v0.3.0 candidate gates, including:

- a clean `--no-local` clone and idempotent standalone initialization;
- an eighteen-record fictional schema-2 graph spanning all seven authorities;
- migration plan, apply, verify, schema-3 full check, and localized view build;
- the full Python suite, Ruff, mypy, fast and full checks, Skill verification,
  and light dual-host harness verification;
- the complete public-history privacy audit with redacted Career Home
  comparison;
- main CI on Windows, Ubuntu, and macOS before the annotated tag; and
- tag-workflow synthetic XeLaTeX builds, preview exports, and final PDF privacy
  audits before GitHub Release creation.

The immutable
[v0.3.1 extraction supplement](v0.3.1-extraction.json) records the exact
roll-forward delta over v0.3.0. The changed release workflow was deliberately
reviewed as synthetic-only before its new blob hash was appended to the guarded
fixture policy.

Automation establishes deterministic framework, installation, migration,
privacy, and release behavior. It does not establish career evidence maturity,
readiness, application success, or any external career outcome.
