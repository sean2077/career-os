# v0.7.0 Release Evidence

Date: 2026-07-27

Release boundary: the reviewed public-bound framework diff from clean Career
Home commit `d62426d7ef0ab3e73ef706cef62744deb3b6abca` was adapted on top of
public `v0.6.0` commit `7881650c234567fdba22da29980ee37c1ed882eb` and
committed as the public-only contribution
`cfd979ec299db39830824b4c560e6bac7bd0dbbb`.

The public adaptation retains `standalone-framework`, the light Agent harness,
synthetic-only fixtures, and the existing public CI/release workflow. It
excludes Home worktree governance, private CI, Vault-local tasks, personal
resume sources, `career/`, identity, attachments, fonts, `.obsidian/`, runtime
state, and generated output.

## Scope

- Keep exactly seven tracked Career Skills while moving four Obsidian and two
  contributor Skills behind an explicit, provenance-checked local onboarding
  contract that never installs or downloads through the Career OS CLI.
- Add read-only `skills status`, verified ignored-state `skills configure`, and
  a versioned `skill_onboarding` result for initialization.
- Consolidate same-purpose project preparation and communication artifacts
  into stable living records without merging independently owned evidence,
  readiness, claims, audits, sessions, or export receipts.
- Upgrade Blind Interviewer output to `resume-interview-probe/2`, require the
  exact visible basis for every current question, and gate project-specific
  follow-ups on a disclosure hook or prior candidate answer.
- Keep public CI and release Harness verification after removing bundled
  contributor snapshots by installing and validating the exact
  manifest-reviewed contributor group in ephemeral ignored project scope for
  Codex, then projecting the same authoritative sources to Claude Code.
- Normalize UTF-8 line endings in reviewed Skill tree digests so identical
  upstream content validates on Windows, Linux, and macOS without weakening
  binary-content checks.

## Candidate verification

| Gate | Result |
| --- | --- |
| `uv sync --locked --all-groups` | PASS |
| `uv run career-os check --fast` | PASS with the documented blind-selection ATTENTION only |
| `uv run career-os check` | PASS with the documented blind-selection ATTENTION only |
| `uv run pytest` | PASS, 247 tests |
| `uv run ruff check .` | PASS |
| `uv run mypy system/tools/career_os` | PASS |
| `uv run career-os skills verify` | PASS with the documented blind-selection ATTENTION only |
| Optional-Skill onboarding status contract | PASS |
| Light dual-host Agent harness verification | PASS |
| Exact `v0.7.0` changelog-note extraction | PASS |
| Complete public-history privacy audit with redacted Career Home comparison | PASS |

The immutable
[v0.7.0 extraction supplement](v0.7.0-extraction.json) records the exact
Home-to-public result over v0.6.0.

Automation establishes deterministic framework, Skill onboarding, interview
question provenance, privacy, and release behavior. It does not establish
career evidence maturity, readiness, application success, or any external
career outcome.
