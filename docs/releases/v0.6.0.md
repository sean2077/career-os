# v0.6.0 Release Evidence

Date: 2026-07-26

Release boundary: the reviewed public-bound framework diff from clean Career
Home commit `55fc0e0edc00b3b4b3accc797e86efeb23abc255` was adapted on top of
public `v0.5.0` commit `6644e74686c912607810e3f6ee2ae67de69db007` and
committed as the public-only contribution
`78f520839852c949edfc2cf5a8e7f717a5cb974e`.

The public adaptation retains `standalone-framework`, the light Agent harness,
synthetic-only fixtures, and the existing public CI/release workflow. It
excludes Home worktree governance, private CI, Vault-local tasks, personal
resume sources, `career/`, identity, attachments, fonts, `.obsidian/`, runtime
state, and generated output.

## Scope

- Add optional QuickAdd choices and guarded scripts for active JD, Company, and
  Engagement review plus explicit Engagement-event recording.
- Add localized interview-pipeline views without inferring or mutating an
  external recruiting event.
- Decompose the internal checker, report project-relative issue paths, batch
  Git record reads, and lock homepage prose through reviewed SHA-256 digests.
- Preserve linked-worktree Vault context without sharing mutable local state.
- Make signed-in BOSS browsing explicitly user-operated and keep session
  material outside Career OS.

## Candidate verification

| Gate | Result |
| --- | --- |
| `uv sync --locked --all-groups` | PASS |
| `uv run career-os check --fast` | PASS with the documented blind-selection ATTENTION only |
| `uv run career-os check` | PASS with the documented blind-selection ATTENTION only |
| `uv run pytest` | PASS, 225 tests |
| `uv run ruff check .` | PASS |
| `uv run mypy system/tools/career_os` | PASS |
| `uv run career-os skills verify` | PASS with the documented blind-selection ATTENTION only |
| Light dual-host Agent harness verification | PASS |
| QuickAdd JavaScript syntax checks | PASS |
| Exact `v0.6.0` changelog-note extraction | PASS |
| Complete public-history privacy audit with redacted Career Home comparison | PASS |

The immutable
[v0.6.0 extraction supplement](v0.6.0-extraction.json) records the exact
Home-to-public result over v0.5.0.

Automation establishes deterministic framework, Obsidian, privacy, and release
behavior. It does not establish career evidence maturity, readiness,
application success, or any external career outcome.
