# v0.8.0 Release Evidence

Date: 2026-08-24

Release boundary: the public-native changes beginning at `4c412d1af4886e2b5bbb3efbac828e54dbdc43d2` apply multi-authority routing and documentation-authority consolidation on top of public `v0.7.1` commit `4add64f15c707e11b7acbff8868f8021d4ad2fcc`.

The public snapshot retains `standalone-framework`, the light Agent Harness, synthetic-only fixtures, and the existing tag-triggered release workflow. No private record, identity, attachment, font, Obsidian state, runtime state, or generated output enters this release.

## Scope

- Expand the blind Skill-selection contract with nuanced multi-authority outcomes while preserving distinct authority outputs and stable handoffs.
- Consolidate active documentation around task-first navigation, one authority for each changing fact, and a visible separation between current guidance and release history.
- Keep maintainer command sequences canonical in `docs/tooling.md` and route README, CONTRIBUTING, and AGENTS readers there instead of maintaining parallel copies.
- Replace brittle natural-language assertions with deterministic route, command, version-authority, and relation-key contracts.
- Restore fail-closed CI by extending the cumulative extraction evidence across every tracked public change since v0.7.1.

## Candidate verification

| Gate | Result |
| --- | --- |
| Offline locked dependency resolution | PASS |
| `career-os check --fast` | PASS with the documented blind-selection ATTENTION only |
| `career-os check` | PASS with the documented blind-selection ATTENTION only |
| Full Python suite | PASS |
| `ruff check .` | PASS |
| `mypy system/tools/career_os` | PASS |
| `career-os skills verify` | PASS with the documented blind-selection ATTENTION only |
| Exact `v0.8.0` changelog-note extraction | PASS |
| Complete extraction-supplement verification | PASS |
| Complete public-history privacy audit with the private Career Home comparison | PASS |
| Clean package build and reviewed light Harness | PASS |
| Hosted Windows, Ubuntu, macOS, and TeX Live fixtures | REQUIRED on `main` before tagging |

The tag workflow repeats the public-only privacy, repository, resume, and release gates before creating the GitHub Release.

The immutable [v0.8.0 extraction supplement](v0.8.0-extraction.json) records the direct public-native delta over v0.7.1 and hash-binds the complete release snapshot.

Automation establishes deterministic framework, Skill routing, privacy, documentation, and release behavior. It does not establish career evidence maturity, readiness, application success, or any external career outcome.
