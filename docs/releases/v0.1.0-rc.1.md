# v0.1.0-rc.1 Local Release Evidence

Date: 2026-07-21

Platform: Windows 11, Python 3.12.13, uv 0.11.29, Git 2.54.0, TeX Live 2025

Release boundary: local annotated Git tag only

## Delivered commits

| Phase | Commit | Subject |
| --- | --- | --- |
| Bootstrap | `4f3dee3` | `chore: scaffold career os system layer` |
| Core system | `8ca74f1` | `feat(core): add data model and system cli` |
| Skills | `6c82b16` | `feat(skills): add career workflows and locked bundles` |
| Obsidian | `cc28e8e` | `feat(obsidian): add reversible vault integration` |
| Resume | `39c5ee5` | `feat(resume): add privacy-safe XeLaTeX exports` |
| Release gates | `a8afc6b` | `ci: add cross-platform release gates` |

This evidence file is part of the release-preparation commit. The annotated
`v0.1.0-rc.1` tag points to that final commit.

## Local verification

| Gate | Result |
| --- | --- |
| `uv sync --locked --all-groups` | PASS, 28 packages checked |
| Ruff | PASS, 26 source files |
| mypy strict | PASS, 26 source files |
| pytest | PASS, 29 tests |
| `career-os skills verify` | PASS, exactly 7 Career + 2 Sean + 5 Obsidian Skills |
| `career-os check --fast` | PASS |
| `career-os check` | PASS |
| light Agent harness verify | PASS, 16 checks including 14 real Skill symlinks |
| deterministic SBOM | PASS, 25 locked runtime/Skill/font components |
| CI YAML parse | PASS |
| repository diff/ignored-state checks | PASS |

The tests cover Unicode filenames and content, BCP 47 metadata, stable IDs,
typed host references and anchors, stale plans, rollback, generated-view
drift, non-Git hosts, independent nested repositories, standard submodules,
bare gitlinks, QuickAdd absence/version/conflicts, Skill selection modes, all
five opportunity blocks, and all three hard-gate categories.

## Standalone and embedded scenarios

A clean local clone was initialized in standalone mode with `en,zh-CN`, built
its views, and passed both normal and host-aware checks. A second clean clone
was embedded as an independent nested repository in a synthetic Git Vault with
an external sibling data root. Attach/apply succeeded, a repeated attach
planned zero operations, detach restored the empty host state, and a typed
identity-plus-heading reference into the surrounding Vault passed
`career-os check --host`.

## Resume evidence

Both synthetic fixtures compiled through `latexmk`/XeLaTeX without TeX warnings,
exported through the privacy boundary, rendered to PNG, and passed visual
inspection as clean single-page A4 documents.

| Fixture | Export SHA-256 | Size | Privacy result |
| --- | --- | ---: | --- |
| English | `f310458b585b1768071d2a9f8af0ec0f09feb92d031436f5294da3d21edeabbe` | 12,649 bytes | PASS |
| Simplified Chinese | `f4087057b40236a60b5609ba3ecd707da0779ac2236199755b01fae794774cbe` | 26,407 bytes | PASS |

Each exported PDF is unencrypted, has one A4 page, no metadata stream, no
links/actions/attachments/images, and only generic `Career OS` creator and
producer fields. Font binaries remained in ignored local state and matched the
pinned Noto Sans CJK 2.004 SHA-256 values.

## CI and publication status

The checked workflow defines Windows, Ubuntu, and macOS core jobs plus an
Ubuntu XeLaTeX fixture/export job. Actions are pinned to complete release commit
SHAs. Hosted CI was not run because repository creation and push are outside
this release's authorization boundary.

No remote is configured. No GitHub repository, push, hosted release, or public
artifact was created. The pre-existing private career repository was not
modified.
